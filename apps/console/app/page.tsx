// Route component — default export permitted per /CLAUDE.md CODE STYLE.
// Intentionally blank: skeleton only, no feature code yet.
export default function HomePage() {
  return <main className="min-h-screen" />;
}
